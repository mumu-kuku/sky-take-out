<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sky.mapper.OrderDetailMapper">
    <insert id="insertBatch">
        insert into order_detail
            (order_id, name, image, amount, dish_flavor, number, setmeal_id, dish_id)
        values
            <foreach collection="orderDetailList" item="orderDetail" separator=",">
                (#{orderDetail.orderId}, #{orderDetail.name}, #{orderDetail.image}, #{orderDetail.amount}, #{orderDetail.dishFlavor}, #{orderDetail.number}, #{orderDetail.setmealId}, #{orderDetail.dishId})
            </foreach>
    </insert>
</mapper>